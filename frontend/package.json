{
  "name": "@muksan-homepage/frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix ../shared run build && vite --host 127.0.0.1 --port 5175",
    "build": "npm --prefix ../shared run build && tsc -p tsconfig.json --noEmit && tsc -p tsconfig.node.json --noEmit && vite build",
    "typecheck": "npm --prefix ../shared run build && tsc -p tsconfig.json --noEmit --pretty false && tsc -p tsconfig.node.json --noEmit --pretty false",
    "preview": "vite preview --host 127.0.0.1 --port 5175"
  },
  "dependencies": {
    "@muksan-homepage/shared": "file:../shared",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.3",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.5.3",
    "vite": "^8.1.0"
  }
}
